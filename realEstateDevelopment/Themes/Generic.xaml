<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:realEstateDevelopment.MVVM.View">


    <ControlTemplate x:Key="AddButtonTemplate" TargetType="{x:Type ContentControl}">
        <Grid Background="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ContentPresenter Content="{TemplateBinding Content}"/>
            <Button Grid.Row="1" Content="Zapisz i zamknij" Style="{DynamicResource CommonNormalButtonTheme}" Command="{Binding SaveCommand}"/>
        </Grid>
    </ControlTemplate>


    <ControlTemplate x:Key="UpdateButtonTemplate" TargetType="{x:Type ContentControl}">
        <Grid Background="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ContentPresenter Content="{TemplateBinding Content}"/>
            <Button Grid.Row="1" Content="Zapisz i zamknij" Style="{DynamicResource CommonNormalButtonTheme}" Command="{Binding UpdateCommand}"/>
        </Grid>
    </ControlTemplate>

</ResourceDictionary>
